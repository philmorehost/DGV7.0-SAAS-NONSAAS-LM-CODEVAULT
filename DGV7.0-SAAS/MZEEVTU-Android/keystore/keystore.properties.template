# Copy this file to keystore.properties and fill in the real values.
# keystore.properties is gitignored to keep credentials out of VCS.
# For CI, set KEYSTORE_BASE64 / KEYSTORE_PASSWORD / KEY_ALIAS / KEY_PASSWORD as GitHub Actions secrets.
STORE_PASSWORD=<your-store-password>
KEY_ALIAS=philmore-mzeevtu
KEY_PASSWORD=<your-key-password>
